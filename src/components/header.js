import * as React from "react"
import { Link } from "gatsby";
import logo from '../Assets/images/logoEl.png'
import Navbar from "./Navbar";

const Header = ({ siteTitle }) => (
  <header>
    <Navbar/>    
  </header>
)

export default Header
